{
  "manifest_version": 3,
  "name": "Tab Number Title Prefix",
  "version": "1.0",
  "description": "Prefixes each tab's title with its index, so you know which Ctrl/Cmd+Number shortcut jumps to it.",
  "permissions": ["tabs"],
  "background": {
    "service_worker": "background.js"
  },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"],
      "run_at": "document_idle",
      "all_frames": false
    }
  ]
}
